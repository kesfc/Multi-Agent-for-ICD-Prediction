[project]
name = "multi-agent-for-icd-prediction"
version = "0.1.0"
description = "Multi-agent ICD prediction prototype"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "pydantic>=2.0.0",
]
